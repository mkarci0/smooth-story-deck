
Bu bir rehberlik isteği — kod değişikliği yok. Mevcut implementasyonu kontrol edip doğru adımları vereceğim.

**Doğrulanacaklar:**
1. `admin.settings.tsx` içindeki "Site Status" sekmesi gerçekten var mı, toggle ne yapıyor
2. `__root.tsx` içindeki maintenance gating mantığı (admin & `?preview=1` bypass)
3. ComingSoon component public ziyaretçilere gösteriliyor mu
